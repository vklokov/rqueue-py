services:
  redis:
    image: public.ecr.aws/docker/library/redis:6.2
    healthcheck:
      test: [ "CMD-SHELL", "redis-cli ping | grep PONG" ]
      interval: 1s
      timeout: 3s
      retries: 5
    volumes:
      - ./tmp/redis:/data
    ports:
      - "6379:6379"